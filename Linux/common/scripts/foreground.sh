#!/bin/bash

tmux

echo  'You are running 'tmux' now, press Ctrl+b " (double quote) or % to split panes' \
'Ctrl+b then DOWN/UP, to navigate between them (Ctrl+b Ctrl+DOWN/UP to resize)' \
'Open manual in one pane and practice in other! (Ctrl+b x to close pane)'
