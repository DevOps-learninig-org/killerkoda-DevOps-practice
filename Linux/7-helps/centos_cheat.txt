# Install
rpm -i *.rpm
# List
rpm -qlp package.rpm
# Unpack
rpm2cpio *.rpm | cpio -idmv

# Create subdirectory in all directories of /catalog
find /catalog/ -maxdepth 1 -type d -exec mkdir -p {}/.script \;

# Change MC editor
select-editor

du -hat 1M / | sort -h | tail

du -hS /data/loader/ | sort -h -r | head -50
du -s /* |sort -nr|cut -f 2-|while read a;do du -hs $a;done - посмотреть что забивает на разделе
